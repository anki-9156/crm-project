pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/anki-9156/crm-project.git'
            }
        }
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying to S3...'
                // Add AWS CLI commands here if needed
            }
        }
    }
}
